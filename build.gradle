apply plugin: 'groovy'
apply plugin: 'maven'

group = 'no.kodehuset.gradle.plugins'
version = '1.1.3'

dependencies {
    compile gradleApi()
    compile localGroovy()
}


task wrapper(type: Wrapper) {
    gradleVersion = '2.14'
}
